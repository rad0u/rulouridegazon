import ProtectedLayout from '../components/ProtectedLayout';

export default function CheltuieliIndirecteLayout({ children }: { children: React.ReactNode }) {
  return <ProtectedLayout>{children}</ProtectedLayout>;
}
